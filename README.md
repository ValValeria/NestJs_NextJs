# Chat app
The app is under development

## Running the app
Type in the terminal the following
```bash
# development
$ cd server
$ npm install
# watch mode
$ npm run start:dev
```
In the separate terminal
```bash
# development
$ cd client
$ npm install
# watch mode
$ npm run start
```
